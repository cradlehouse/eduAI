# infra — placeholder

- `render.yaml` (orchestrator web + 2 workers) — P1-16
- `cloudflare/webhook-inbox/` (Worker: verify signature → insert into `webhook_inbox` → 200) — P1-11
- `r2-lifecycle.json` — P1-10
