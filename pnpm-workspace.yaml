packages:
  - apps/*
  - packages/*
  - infra/cloudflare/*
# Native postinstall builds we trust (pnpm 11 blocks all by default).
allowBuilds:
  esbuild: true
  unrs-resolver: true
  workerd: true
  sharp: true
  '@sentry/cli': false   # sourcemap uploader; not used, never built
